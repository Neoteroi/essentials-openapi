# https://github.com/Neoteroi/essentials-openapi/issues/24
openapi: "3.0.2"
info:
  title: API Title
  description: |
    **Response**

    ```json
    [
      {
        "a": "b",
        "c": {
          "d": "e"
        }
      }
    ]
    ```
  version: "1.0"
servers:
  - url: https://api.server.test/v1
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
